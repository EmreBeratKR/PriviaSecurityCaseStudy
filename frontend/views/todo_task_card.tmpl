<div class="task-card">
  <div class="task-left">
    {{if .AllowEditting}}
      <form method="POST" action="/todo-task/patch?redirect_id={{.TodoListId}}">
        <input name="id" value="{{.Id}}" hidden>
        <input name="action" value="toggle" hidden>
        <button class="toggle {{if .IsCompleted}}checked{{end}}" type="submit">
          {{if .IsCompleted}}✓{{end}}
        </button>
      </form>
    {{else}}
      <button class="toggle {{if .IsCompleted}}checked{{end}}" disabled>
        {{if .IsCompleted}}✓{{end}}
      </button>
    {{end}}
    <div class="task-desc">{{.Content}}</div>
  </div>
  {{if .AllowEditting}}
    <div class="task-actions">
      <form method="GET" action="/todo-list">
        <input type="hidden" name="id" value="{{.TodoListId}}">
        <input type="hidden" name="edit_todo_task_id" value="{{.Id}}">
        <button class="action-btn edit-btn" type="submit">
          <img src="/icons/edit.svg" alt="Edit" width="16" height="16">
        </button>
      </form>
      <form method="POST" action="/todo-task/delete?redirect_id={{.TodoListId}}">
        <input type="hidden" name="id" value="{{.Id}}">
        <button class="action-btn delete-btn" type="submit">
          <img src="/icons/trash.svg" alt="Delete" width="16" height="16">
        </button>
      </form>
    </div>
  {{end}}
</div>
